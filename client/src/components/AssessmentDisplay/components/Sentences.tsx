import { motion } from 'framer-motion';
import styled from 'styled-components';

const Sentences = styled(motion.ul)`
  list-style-type: none;
  background-color: grey;
  overflow: hidden;
  padding: 0;
`;

export default Sentences ;